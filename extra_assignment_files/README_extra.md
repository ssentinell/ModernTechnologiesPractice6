# Дополнительные файлы

Этот каталог я добавил отдельно, потому что в исходном архиве не было заготовок для части файлов.

Здесь лежат:

- `part2_dao/` — реализация DAO-паттерна
- `part3_hibernate/` — версия задания с отдельными `Movie.java`, `HibernateMain.java` и `hibernate.cfg.xml`
- `part4_transactions/` — JDBC-транзакции для перевода средств между счетами
